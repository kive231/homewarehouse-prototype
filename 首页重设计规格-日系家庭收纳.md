# 首页重设计规格：日系家庭收纳

## 目标

将家庭仓库首页从“库存工具看板”调整为温润、有序、适合每日查看的家庭收纳总览。页面只服务三件事：判断库存状态、了解当日流动、进入高频操作。

## 设计哲学

采用原研哉式的留白与克制：信息清楚但不压迫，主色只作为识别与行动引导；不使用企业化指标墙、深色大色块或装饰性图形。图标均使用现有线性 SVG，禁止 emoji。

## 范围与边界

- 仅重设计 `home-status-dial-preview.html` 的手机端首页。
- 保留现有信息结构、底部导航、扫码入口和批注模式。
- 不新增业务功能、不改变数据字段、不制作平板或桌面布局。

## 视觉 Token

| 用途 | 色值 |
| --- | --- |
| 主色（浅紫） | `#6F7DCC` |
| 浅紫容器 | `#F1F3FF` |
| 页面背景（暖米白） | `#F7F5F0` |
| 卡片 | `#FFFEFB` |
| 正文 | `#30313A` |
| 辅助文字 | `#85838A` |
| 正常 | `#77A99C` |
| 临期 | `#D9A16F` |
| 低库存 | `#D77778` |

色彩比例：暖米白与卡片约 90%，紫色仅用于品牌识别、当前导航、扫码和关键图标，功能色仅用于状态。

## 布局与层级

页面采用 8pt 网格、左右 16px 边距；主要内容按以下顺序排布：

1. 标题和搜索入口：安静、无装饰。
2. 库存状态总览：全页唯一主卡，展示物品总数与正常/临期/低库存。
3. 今日流动：展示入库、出库、净增，作为次级信息。
4. 快捷操作：新增、入库、出库，触摸目标不小于 44pt。
5. 需要处理：最多两条风险提醒，其他进入“全部”。
6. 浮动底部导航：中间扫码按钮为唯一凸起的高频入口。

核心状态和快捷操作应在常见 390×844 手机屏幕首屏可见；待办允许轻微下滑查看。

## 组件规则

- 总览卡圆角 24px；普通卡片 18px；小状态块 14px。
- 卡片使用 1px 暖灰边框与极轻阴影，不嵌套厚阴影。
- 文字仅使用系统字体；标题 600，正文 500，辅助信息 400。
- 主要正文不低于 16px；辅助文案可为 12px。
- 动效限定为 200–300ms，并响应 `prefers-reduced-motion`。

## 验收标准

- 页面仅呈现手机原型，不随桌面宽度变为网页布局。
- 首屏不出现复杂状态盘、趋势图、六宫格功能墙或 emoji。
- 紫色比旧 `#5B6ABF` 更轻，但视觉焦点仍清晰。
- 关键按钮和文字达到可读对比度；可点击区域不小于 44px。
- 批注模式保持可用，且所有既有主要区域仍可批注。

