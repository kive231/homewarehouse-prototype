# 家庭仓库详情页与 CRUD Pencil 实施计划

## 一句话目标

按业务依赖顺序在现有 Pencil 文件中完成物品、空间分类、扫码流水和设置的详情/CRUD移动端原型，并保持 6 个一级页面的全局视觉规范。

## 修改范围

- 修改：`C:\Users\zp.wu\.pencil\documents\0c9a279a-276b-4863-b123-d2bda89a0f47\pencil-new.pen`
- 新增规格：`D:\house_ware\homewarehouse-prototype-huashu-redesign\docs\plans\2026-07-16-detail-crud-design.md`
- 新增计划：`D:\house_ware\homewarehouse-prototype-huashu-redesign\docs\plans\2026-07-16-detail-crud-implementation-plan.md`
- 不修改：现有 HTML 原型、Flutter 代码、后端与数据库。

## 全局约束

- 手机画布 320 × 682。
- 新增根页面先设置 `placeholder: true`，完成并验证后改为 `false`。
- 详情/表单页使用顶部返回，不显示一级底部导航。
- 字体、色板、圆角、阴影和图标统一。
- 每完成一个页面立即运行 Pencil 布局校验；每阶段结束做全局不明图标扫描。
- 不并行制作多个未完成页面，按单页顺序完成。

## 执行任务

### [x] 1. 建立详情页公共骨架与跳转地图

- 输入：现有 6 个一级页、设计规格、Pencil 变量。
- 产物：详情页顶部栏、返回按钮的公共结构；一级页到详情页的跳转地图。
- 验证：读取新增节点；确认标题和返回入口命名完整；布局无裁切。
- 预期：后续页面可以复制骨架，避免样式分叉。

### [x] 2. 完成物品详情页

- 输入：物品页第一条“温莎牛顿 24 色颜料”。
- 产物：核心信息、库存、风险、二维码状态、操作入口、最近流水。
- 验证：Pencil `snapshot_layout(problemsOnly:true)` 返回无问题；视觉检查信息层级与按钮触摸区。
- 预期：物品列表和扫码成功态都能指向同一详情结构。

### [x] 3. 完成新增物品与编辑物品页

- 输入：Item 字段和现有 HTML 表单逻辑。
- 产物：空白新增态、预填编辑态、字段校验态、保存按钮、归档/删除入口。
- 验证：字段覆盖名称、末级分类、末级位置、数量、单位、阈值、保质期、二维码；无字段溢出。
- 预期：新增保存进入详情，编辑保存返回更新后的详情。

### [x] 4. 完成消耗、补货与删除/归档操作层

- 输入：当前库存 3 盒、阈值 2 盒。
- 产物：消耗操作层、库存不足态、补货操作层、删除/归档确认态。
- 验证：数量预览和危险操作层级明确；确认/取消触摸区不小于 44px。
- 预期：轻量操作不新增多余页面，结果能回写详情信息与流水。

### [x] 5. 完成移动位置选择页

- 输入：空间树“家 → 书房 A 柜 → 2层 → 蓝色颜料盒”。
- 产物：原位置、递进选择、已选目标、确认移动。
- 验证：只有末级盒子可确认；面包屑和返回路径清楚；无复杂整树展示。
- 预期：移动成功后详情页位置与流水同时刷新。

### [x] 6. 完成空间下钻、盒子详情和空间节点表单

- 输入：现有空间页列表及左滑编辑规则。
- 产物：柜子/层下钻态、盒子详情、新增空间节点、编辑空间节点、删除受阻态。
- 验证：拖动、点击、左滑、返回四种交互语义不冲突；关联物品时不能直接删除。
- 预期：空间 CRUD 与末层物品入口完整。

### [x] 7. 完成分类下钻、细类详情和分类表单

- 输入：现有分类树和三级限制。
- 产物：大类/子类下钻态、细类详情、新增分类、编辑分类、删除受阻态。
- 验证：最大三级；末层进入物品列表；图标全部为已知图标库资源。
- 预期：分类 CRUD 与空间页面共享视觉骨架但保持语义差异。

### [x] 8. 完成扫码结果与库存流水详情

- 输入：二维码 `target_type` 路由和库存流水字段。
- 产物：识别中、物品码成功、位置码成功、无法识别、流水详情。
- 验证：两类成功结果跳转目的明确；失败态支持重扫与手输。
- 预期：扫码和流水不再是流程断点。

### [x] 9. 完成“我的”下级设置页面

- 输入：家庭与成员、提醒设置、数据与导出、关于帮助四个入口。
- 产物：四类详情页的关键状态和主要操作。
- 验证：设置项使用统一列表/表单规范；危险操作与普通设置分区。
- 预期：“我的”一级页的所有可见入口都有承接页面。

### [x] 10. 全局审查与交付

- 输入：全部一级页与新增页面。
- 产物：完整页面清单、跳转清单、全局视觉校验结果。
- 验证：
  - 所有根页面运行 `snapshot_layout(problemsOnly:true)`。
  - 全局扫描未知字符图标，结果为 0。
  - 抽查首页、物品详情、空间详情、扫码结果和设置页截图。
  - 检查页面尺寸、字体、主色、按钮和返回入口。
- 预期：无遗漏页面、无非预期裁切、无样式分叉、无无法识别图标。

## 回滚方式

- Pencil 每一阶段在独立页面节点中完成；失败时删除该阶段新增根页面，不修改已完成一级页。
- 文档变更通过对应 Git 提交反向提交，不使用强制重置。

## 最终验收标准

- 物品、空间、分类、扫码、流水和设置的详情/CRUD跳转全部有原型承接。
- 全部新页面与现有 6 个一级页面保持统一视觉语言。
- 物品库存变动、空间分类维护、扫码路由形成闭环。
- 全局校验清单明确页面总数、统一规范和逐页结果。

## 执行结果

- 完成 29 个移动端页面：6 个一级页与 23 个详情、CRUD、操作状态页。
- 建立 1 个可复用公共组件：详情页顶部栏。
- 建立 1 个详情与 CRUD 跳转地图。
- 全部页面统一为 320 × 682，并统一字体、色板、圆角、阴影和 Lucide/Feather 图标。
- 29 个页面逐页布局校验完成：24 页无问题；5 页仅存在设计需要的流体丝带裁切；无非预期裁切、溢出或重叠。
- 全局不明字符图标扫描结果为 0。
- 按最新评审移除全部页面批注入口及公共批注组件。
