# 家庭仓库首页原型设计思路与方案

> 日期：2026-07-12  
> 范围：家庭仓库首页原型、整体视觉方向、核心组件与批注迭代记录  
> 当前原型文件：`home-status-dial-preview.html`

## 1. 设计目标

首页不是功能入口堆叠页，也不是企业 WMS 看板，而是家庭仓库的状态总览页。

首页需要让用户快速判断三件事：

```text
1. 家里的库存现在是否正常
2. 今天有什么库存流动
3. 当前最需要处理什么
```

设计关键词：

```text
家庭感
温润
有序
图标优先
少文字
少大数字
轻报表
快速操作
```

整体方向一句话：

```text
用柔和的紫蓝色、图标状态和轻量数据，做一个家庭库存状态总览。
```

## 2. 已确认的产品认知

### 2.1 首页不做复杂功能墙

早期讨论中，首页曾拆成 6 类内容：

```text
总览指标
趋势报表
存储分布
异常与待处理
快速操作
最近活动
```

后来判断：如果 6 类同级展示，首页会过重，也会像后台系统。

最终收敛为 3 层：

```text
1. 库存状态
2. 今日流动
3. 处理入口
```

### 2.2 用户更偏好图标而不是文字和数字

用户明确表示更喜欢看图标，不希望首页堆满文字和数字。

因此首页表达规则调整为：

```text
先看图标
再看颜色
最后看数字
```

数字不作为主视觉，只在必要时用于：

```text
数量标记
状态补充
流水汇总
```

### 2.3 二维码不是绑定逻辑的核心

已纠正业务概念：

```text
创建物品后自动生成 ItemCode
ItemCode 是物品唯一编码
二维码由 ItemCode 生成
扫码后解析 Code，再定位物品
```

因此设计上应强调 Code / 标签，而不是“绑定二维码”。

## 3. 视觉方向

### 3.1 主色调

主色继续使用紫蓝：

```text
主色：#5b6abf
辅助紫：#8b94d6
浅紫底：#eef0fb
```

功能色：

```text
正常：#5baa9e
临期 / 注意：#e8a87c
过期 / 严重：#d9626e
中性灰：#8b90a0
```

背景与文本：

```text
页面背景：#f8f9fc
卡片背景：#ffffff
主文字：#252736
次文字：#8b90a0
分割线：#eceef5
```

### 3.2 色彩使用原则

```text
70% 浅背景
15% 白色卡片
10% 紫蓝主色
5% 功能色
```

主色不大面积铺满，只用于：

```text
库存主图标
底部导航选中态
扫码按钮
关键操作图标
Code / 标签视觉
```

### 3.3 不继续使用的方向

不再使用旧青绿作为主色：

```text
#3ba99c 禁止作为主色
```

避免以下视觉元素：

```text
大面积青绿色渐变
企业后台式指标卡
复杂折线图首屏展示
过多文字解释
六宫格以上快捷入口
emoji 图标
过重玻璃拟态
过重阴影
```

## 4. 首页结构方案

当前首页结构：

```text
顶部标题与搜索入口
-> 库存状态总览卡
-> 今日流动
-> 快捷操作：新增 / 入库 / 出库
-> 需要处理
-> 底部浮动胶囊导航
```

### 4.1 顶部标题与搜索入口

设计目标：轻量、安静，不做强装饰。

当前规则：

```text
标题：家庭仓库
副标题：状态总览
搜索图标背景与页面背景一致
搜索按钮不做白色高亮卡片
```

原因：用户批注认为搜索背景应当和当前页面背景一致，避免右上角显得突兀。

### 4.2 库存状态总览卡

最初尝试过“图标状态盘”，包括中心物品图标和多个环绕图标。

用户反馈：

```text
什么东西，乱七八糟的？！
```

因此废弃环绕式状态盘，改为更直接的库存总览卡。

当前内容：

```text
128 件物品 · 今日整体稳定
正常 120
临期 3
低库存 5
```

设计原则：

```text
一个主图标
一个核心数字
三个状态块
不再做环绕图标
不再做复杂装饰
```

用户后续批注：

```text
卡片右上角的圆形背景不要
```

因此已去掉总览卡右上角圆形背景装饰。

### 4.3 今日流动

原先叫“趋势带”，包含：

```text
库存增加
消耗平稳
工具柜偏满
```

用户反馈：

```text
太丑了，简约一些，而且这个没什么意义吧
```

因此不再做抽象趋势判断，改为更直接的库存流水汇总：

```text
今日流动
+12 入库
-3 出库
+9 净增
```

设计原因：

```text
入库 / 出库 / 净增 与库存业务直接相关
用户能一眼理解
比抽象“趋势”更有意义
```

后续批注：

```text
高度高一些，下面的尽可能做正方形
```

因此已调整：

```text
今日流动卡片整体高度增加
三个数据块使用接近正方形比例
圆角和间距同步放大
```

### 4.4 快捷操作

最初设计了 6 个快捷操作：

```text
入库
消耗
补货
移动
扫码
调整
```

用户反馈：

```text
不要这么多，新增，出库入库即可
```

当前保留 3 个操作：

```text
新增
入库
出库
```

设计原则：

```text
首页只放最高频动作
其他操作进入详情页或二级页面
不要把首页做成功能菜单
```

### 4.5 需要处理

当前保留轻量风险列表：

```text
临期物品提醒
低库存物品提醒
```

示例：

```text
温莎牛顿 24 色颜料：30 天后到期
7 号电池：只剩 1 节
```

设计原则：

```text
只展示最该处理的事项
列表不要过长
用图标和颜色提示状态
```

### 4.6 底部导航

保留原有浮动胶囊导航方向。

结构：

```text
首页
空间
扫码
物品
我的
```

中间扫码按钮凸起，继续作为家庭仓库的高频入口。

## 5. 组件规则

### 5.1 卡片

```text
白底
轻阴影
大圆角
浅边框
避免嵌套卡片
```

建议规格：

```text
普通卡片圆角：18-22px
总览卡圆角：24-26px
小状态块圆角：16-18px
```

### 5.2 图标

图标用于表达状态和动作，不做装饰性堆叠。

当前建议图标：

```text
物品：Package / Box
临期：Clock
低库存：Alert
入库：ArrowDown / In
出库：ArrowUp / Out
空间：Cabinet / Warehouse
搜索：Search
扫码：Scan
```

规则：

```text
图标必须服务于业务含义
不要为了好看随便加图标
不要一屏放太多不同形态的图标
```

### 5.3 数字

数字可以出现，但不应成为页面唯一表达。

使用场景：

```text
物品总数
状态数量
入库 / 出库 / 净增
```

避免：

```text
满屏大数字
多个统计卡平铺
没有业务动作含义的数字
```

### 5.4 操作按钮

首页操作按钮当前只保留：

```text
新增
入库
出库
```

按钮样式：

```text
白底
图标在上
文字在下
高度稳定
接近方形但不做拥挤
```

## 6. 批注模式

当前原型保留批注模式。

使用方式：

```text
点击右下角「批注模式」
-> 点击页面区域
-> 输入批注
-> 保存批注
-> 复制给小洛
```

每个主要区域都设置了 `data-annotation`，方便用户指出具体问题。

当前可批注区域包括：

```text
顶部标题与搜索入口
库存状态总览卡
正常 / 临期 / 低库存状态块
今日流动
快捷操作栏
需要处理
底部浮动胶囊导航
```

## 7. 本轮已形成的设计决策

### 决策 1：首页从状态盘改为状态总览卡

原因：状态盘视觉复杂，用户理解成本高。

结论：保留图标化方向，但不做环绕式结构。

### 决策 2：首页不再强调抽象趋势

原因：“库存增加 / 消耗平稳 / 空间偏满”这类抽象趋势意义不够直接。

结论：改成“今日流动”，用库存流水汇总表达入库、出库、净增。

### 决策 3：首页快捷操作只保留 3 个

原因：首页不能变成功能菜单。

结论：只保留新增、入库、出库。

### 决策 4：装饰要克制

原因：右上圆形背景等装饰会干扰主要内容。

结论：总览卡去除非必要装饰。

### 决策 5：搜索入口融入背景

原因：搜索是工具入口，不应抢主视觉。

结论：搜索图标背景与页面背景一致，去掉强卡片感。

## 8. 后续可继续探索

### 8.1 首页报表是否继续下沉

当前首页只保留轻量数据。

后续可以考虑单独做“报表详情页”：

```text
库存变化趋势
分类分布
空间分布
入库 / 出库记录
```

首页只做摘要，不承担完整报表。

### 8.2 今日流动的数据口径

建议后端对应库存流水统计：

```text
今日入库数量
今日出库数量
今日净增数量
```

可按自然日统计，也可按最近 24 小时统计。建议第一版用自然日。

### 8.3 新增 / 入库 / 出库的业务边界

当前首页保留三个动作，但需要后续明确：

```text
新增：创建从未存在过的物品
入库：已有物品增加库存
出库：已有物品减少库存
```

这三个动作在交互和后端接口上应保持区分。

## 9. 当前文件

当前原型文件：

```text
/home/dev/仓库原型/home-status-dial-preview.html
```

建议后续继续在该文件上通过批注模式迭代。

## 10. 当前阶段结论

首页方向已经从“看起来有设计”收敛为“业务上更清楚”：

```text
库存状态总览
今日流动
新增 / 入库 / 出库
需要处理
```

这比原来的图标状态盘更简单，也更符合家庭仓库首页的第一版目标。
